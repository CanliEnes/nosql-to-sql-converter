# nosql-to-sql-converter
"An autonomous Python-based engine that dynamically parses unstructured NoSQL JSON data, normalizes it (1NF, 2NF, 3NF), and converts it into relational SQL database architectures."
